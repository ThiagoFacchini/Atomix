// @flow
import { createSelector } from 'reselect'

//
// Direct selector to the {{ camelCase name }} state domain
//
const select{{ properCase name }}Domain = () => (state: Object) => state.{{ camelCase name }}

//
// Other specific selectors
//


//
// Default selector used by {{ properCase name }}
//

const selectView{{ properCase name }} = () => createSelector(
  select{{ properCase name }}Domain(),
  (substate) => substate
)

export default selectView{{ properCase name }}

export {
  select{{ properCase name }}Domain,
}
